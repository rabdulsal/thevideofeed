Today's <%= @videos.count %> hand-picked <%= @videos.many? ? 'videos' : 'video' %>:

<% @videos.each do |video| %>
<%= video %>
<% end %>

-- The Video Feed

<%= root_url %>

Unsubsribe by clicking here:

<%= unsubscribe_url(@subscriber.key) %>


