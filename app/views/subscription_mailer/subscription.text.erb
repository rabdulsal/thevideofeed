Today's <%= @videos.count %> <%= @videos.many? ? 'videos' : 'video' %>:

<% @videos.each do |video| %>
<%== video.title %> from <%== video.first_person.name %>
<%= video_link(video) %>

<% end %>
-- The Video Feed
<%= root_url %>

<%- if @subscriber.daily? %>
Change to a weekly email:
<%= subscribe_weekly_url(@subscriber.key) %>
<%- else %>
Change to a daily email:
<%= subscribe_daily_url(@subscriber.key) %>
<%- end %>

Unsubscribe:
<%= unsubscribe_url(@subscriber.key) %>
