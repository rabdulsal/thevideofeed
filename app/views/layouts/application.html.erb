<!DOCTYPE html>
<html>
  <head>
    <title>The Video Feed</title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= image_tag('logo.png') %>
      <h1><%= link_to 'The Video Feed', root_path %></h1>

      <p>YouTube videos that have been favorited by:</p>

      <div id="people">
        <% Person.all.each do |person| %>
          <p><%= link_to person.username, "/#{person.username}" %></p>
        <% end %>
      </div>

      <div id="subscribers">
        <% if flash[:notice] %>
          <p><%= flash[:notice] %></p>
        <% else %>
          <%= form_for(Subscriber.new) do |f| %>
            <p>Sign up to get an email at 5pm if new videos are added:</p>
            <p>
              <%= f.email_field :email, placeholder: 'you@example.com' %>
              <%= f.submit "Sign up" %>
            </p>
          <% end %>
        <% end %>
      </div>
    </header>

    <article>
      <%= yield %>
    </article>
  </body>
</html>
