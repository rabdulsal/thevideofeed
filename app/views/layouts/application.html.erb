<!DOCTYPE html>
<html>
  <head>
    <title>The Video Feed</title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= image_tag('logo.png') %>
      <h1><%= link_to 'The Video Feed', root_path %></h1>

      <% Person.all.each do |person| %>
        <%= link_to person.username, "/#{person.username}" %>
      <% end %>
    </header>

    <article>
      <%= yield %>
    </article>
  </body>
</html>
