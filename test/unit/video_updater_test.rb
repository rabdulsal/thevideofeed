require 'test_helper'

class VideoUpdaterTest < ActiveSupport::TestCase
  test "perform updates favorites for all users" do
    pending
  end

  test "creates favorites using the key and the favorited at timestamp" do
    pending
  end

  test "breaks when encountering a video that's already been favorited" do
    pending
  end
end
